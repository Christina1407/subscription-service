spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/usersub
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yml
logging.level.root: INFO
